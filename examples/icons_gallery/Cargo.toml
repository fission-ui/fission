[package]
name = "icons_gallery"
version = "0.14.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
fission = { path = "../../crates/authoring/fission", default-features = false, features = ["desktop", "icons-reflection"] }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
fission-test-driver = { path = "../../crates/tools/fission-test-driver" }
image = "0.25"

[package]
name = "store-sqlite"
version = "0.14.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
anyhow = "1"
fission = { path = "../../crates/authoring/fission", default-features = false, features = ["desktop", "store-sqlite-native"] }

[package]
name = "fission-layout"
version = "0.11.1"
edition = "2021"
license = "MIT"
repository = "https://github.com/fission-ui/fission"
description = "Layout engine primitives for Fission widget trees"
categories = ["gui", "rendering"]
keywords = ["fission", "layout", "ui-framework", "widgets", "cross-platform"]
homepage = "https://fission.rs"
documentation = "https://fission.rs"
readme = "README.md"

[package.metadata.fission]
platforms = ["android", "ios", "linux", "macos", "windows", "web", "terminal", "static-site", "ssr"]

[dependencies]
fission-ir = { path = "../fission-ir", version = "0.11.1" }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
fission-diagnostics = { path = "../../tools/fission-diagnostics", version = "0.11.1" }

[dev-dependencies]
serde_json = "1.0"

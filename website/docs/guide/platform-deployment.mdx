---
title: Platform and deployment
description: Keep behavior stable while preparing outputs.
---

# Platform and deployment

Use one architecture and one behavior model, then apply platform output steps.

Recommended path:

- implement and stabilize app behavior,
- validate desktop,
- validate web/WASM,
- build responsive mobile web checks,
- keep iOS/Android native packaging out of the current runtime scope until native targets are officially added,
- confirm the same behavior across environments.

Keep environment-specific values out of reducer logic. Put them in deployment configuration and target descriptors.

This pattern is what helps behavior remain consistent as your target list grows.
